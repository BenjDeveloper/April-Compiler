fn resta(a:int, b:int)
{
    a-b;
}

fn div (a:int, b:int)
{
    println(101);
    if (b == 0)
    {
        return -1;
        println(201);
    }
    println(301);
    return a/b;
}

fn foo(l:list)
{
    i := 0;
    for i < l.size()
    {
        println(l[i]);
        l[i] = 45;
        i += 1;
    }
    println(l.size());
}