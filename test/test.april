
fn main()
{
    X:="";
    Y:="XY";
    Z := "QWE";
    V := "AEIO";
    i:= 0;
    println("",X);
    //println(X.len());
    //println(X[i], X[i].len());

    println("",Y);
    println(Y.len());
    println(Y[i], Y[i].len());

    println("",Z);
    println(Z.len());
    println(Z[i], Z[i].len());

    println("",V);
    println(V.len());
    println(V[i], V[i].len());

    // for (i<=x.len())
    // {
    //     y=x[i] + y;
    //     i=i+1;
    // }
    // println(y);
}

main();