global array:list = [8, 6, 8, 7, 78, 60];

fn foo(value:int)
{
    println("value: "+str(value));
}

fn main()
{
    casa := range(0, 5);
    // println(l0[0]);
    // l1 := range(0, 5);

    for i in casa {
        println("hola: "+str(i));
    }

    for i in array
    {
        println("i: "+str(i+6));
        foo(i);
    }
}

main();