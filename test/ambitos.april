// **********************************************************
// **                                                     ***
// **    Programado por Pandicorn & Kenshin Urashima      ***
// **    Test: ambitos en April                           ***
// **                                                     ***
// **********************************************************

fn main()
{
    x := 5;

    if true
    {
        k := 1;
        println("then x: "+toString(x));
        println("then k: "+toString(k));
        if false 
        {
            j := 6;
            println("then then j: "+toString(j));
        }
        else 
        {
            j := 9;
            println("then else j: "+toString(j));

            if false 
            {
                h := 6;
                println("then else then j: "+toString(j));
            }
            else 
            {
                q := 0;

                w := 0;
                for (q < 3)
                {
                    w = 0;
                    println("q -->: "+toString(q));
                    for (w < 3) 
                    {
                        println("w: "+toString(w));
                        w += 1;
                    }
                    // println("z: "+toString(z)); variable no definida
                    q += 1;
                }
                println("q: "+toString(q));
                // println("w: %d", w); variable no definida
            }
            //println("h: %d", h); error
        }
    }
    else 
    {
        j := 0;
        println("else x: "+toString(x));
        println("else j: "+toString(j));
    }
    // manejo de ambitos
    k := 0;
    println("k: "+toString(k));
    // println("j: %d", j); error: variable no definida
}

main();