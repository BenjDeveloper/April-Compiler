// **********************************************************
// **                                                     ***
// **    Programado por Pandicorn & Kenshin Urashima      ***
// **    Test: ambitos en April                           ***
// **                                                     ***
// **********************************************************

fn main():void
{
    x := 5;

    if true
    {
        k := 1;
        _println("then x: %d", x);
        _println("then k: %d", k);
        if false 
        {
            j := 6;
            _println("then then j: %d", j);
        }
        else 
        {
            j := 9;
            _println("then else j: %d", j);

            if false 
            {
                h := 6;
                _println("then else then j: %d", j);
            }
            else 
            {
                {
                    q := 0;

                    for (q < 3)
                    {
                        w := 0;
                        
                        _println("q -->: %d", q);
                        for (w < 3) 
                        {
                            o := 0;
                            _println("w: %d", w);
                            w += 1;
                        }
                        // _println("z: %d", z); variable no definida
                        q += 1;
                    }
                    _println("q: %d", q);
                    // _println("w: %d", w); variable no definida
                }
            }
            //_println("h: %d", h); error
        }
    }
    else 
    {
        j := 0;
        _println("else x: %d", x);
        _println("else j: %d", j);
    }
    // manejo de ambitos
    k := 0;
    {
        j := 1;
        {
            i := 2;
            _println("i: %d", i);
            _println("k: %d", k);
            _println("j: %d", j);
        }
        //_println("i: %d", i); error: variable no definida
        _println("k: %d", k);
        _println("j: %d", j);
    }
    //_println("i: %d", i); error: variable no definida
    _println("k: %d", k);
    // _println("j: %d", j); error: variable no definida
}