// **********************************************************
// **                                                     ***
// **    Programado por Pandicorn & Kenshin Urashima      ***
// **    Test: raiz cuadrada de Newton en April           ***
// **    result: 1.41421                                  *** 
// **********************************************************

//global x:string = sqr(2);

fn sqr(x:int):double
{
    _println("x: %d",x);
    z := 1.0;
    cont := 0;

    for cont < 10
    {
        z = z-(((z*z)-x)/(2*z));
        _println("cont: %d", cont);
        cont++;
    }
    return z;
}

fn main():void
{
    _println("result: %f", sqr(2));
}

main();